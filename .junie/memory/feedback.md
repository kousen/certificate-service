[2026-03-14 18:37] - Updated by Junie
{
    "TYPE": "correction",
    "CATEGORY": "Visual outcome mismatch",
    "EXPECTATION": "The newly added gag features should produce a visibly different generated certificate, not one that looks the same as before.",
    "NEW INSTRUCTION": "WHEN features are implemented on a non-main branch THEN tell how to switch branches and rebuild."
}

